/**
 * Registry entrypoint: `ShellRegistry` loads `shells/minimal/preview`.
 * Keeps behavior identical to `./index.ts` without duplicating the shell class.
 */
export { MinimalShell, createShell, default } from "./index";
